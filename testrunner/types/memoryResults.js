/**
 * @typedef {Object} MemoryResult
 * @property {number} heap - The memory heap size.
 * @property {number} create - The memory creation time.
 * 
 * @typedef {Object} MemoryResults
 * @property {string} framework - The framework name.
 * @property {MemoryResult} results - The memory benchmark results.
 */
export {}